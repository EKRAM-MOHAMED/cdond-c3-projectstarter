---
- name: "install dependencies."
  become: true
  apt:
    name: "tar"
    state: latest
    update_cache: yes

- name: "install pm2"
  become: true
  npm:
    name: pm2
    global: yes
    production: yes
    state: present

- name: move files to backend
  copy:
    src: /home/backend.tar.gz
    dest: /home/ubuntu/backend.tar.gz

- name: Start files
  shell: |
    cd /home/ubuntu/
    tar xzvf backend.tar.gz
    rsync -a home/circleci/project/backend .

- name: Install Node modules
  shell: |
    cd /home/ubuntu/backend
    npm install
- name: Building backend service
  shell: |
    cd /home/ubuntu/backend
    npm run build
    npm run prestart:prod
- name: Executing node
  become: true
  shell: |
    cd /home/ubuntu/backend
    pm2 start npm --name backend -- start
    pm2 ls
- name: start pm2 for dist/main.js
  become: no
  shell: pm2 start main.js
  args:
    chdir: /home/ubuntu/backend/dist
